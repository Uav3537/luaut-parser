-- Imported by modules.luaut.

export function clamp(x: number, lo: number, hi: number): number
    if x < lo then return lo end
    if x > hi then return hi end
    return x
end

export function lerp(a: number, b: number, t: number): number
    return a + (b - a) * t
end
